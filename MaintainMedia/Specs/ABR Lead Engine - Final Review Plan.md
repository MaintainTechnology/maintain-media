---
title: "ABR Lead Engine - Final Review Plan"
project: Maintain Media
version: "4.0"
synced: 2026-09-09
source: "llm-council/runs/20260908-abr-adjudication/final-plan.md"
source_sha256: 523e007e3b6f5728f67e3a75267153a0e7ede1b511456ac5e5885fd059b56adf
tags: [abr-lead-engine, maintain-media]
---
> Synced from the repository; local document links adapted for Obsidian.
> [[ABR Lead Engine - Build Hub|Open the build hub]]

# Scores

Final native fallback specification judgment: **9.3/10**, weighted total **9.275**. Correctness/evidence 9.4×25%; requirements/acceptance 9.3×20%; architecture/data integrity 9.2×20%; privacy/security/operability 9.3×20%; scope/delivery/traceability 9.1×15%. See `judge.md` for reviewed source hashes and rationale. Earlier CLI reports scored an earlier snapshot 8.6; no unanimous final-model score is claimed.

# Comparative Analysis

Merge the useful intent of both anonymous reports: preserve evidence discipline, make uncertain states block the affected capability, bind current authority to every controlled action and require reproducible acceptance. Reject duplicate requests for controls already specified, including source-mapping hold, disabled missing-rule classifier, live suppression checks, CRM uncertainty reconciliation, DNS pinning, prepaid reservations and capacity admission.

The final package is build-planning ready. An absent future application, pending qualified approval or missing production source fixture is not itself a documentation defect when its required evidence, owner, blocked capability and safe fixture behaviour are defined.

# Missing Steps

**No remaining substantive documentation blocker was identified.** Future implementation and release evidence remain outstanding. Complete the existing G1–G7 records only with actual qualified policy decisions, approved source/rule evidence, security/hosting/restore/capacity results, wash/action integration, vendor sandbox evidence, pilot decision and final release evidence. Keep corresponding capabilities off while prerequisites are absent.

# Contradictions

The final revision resolves the identified channel/relevance, retained-identity and content-hash mismatches. It also separates historical content reuse from publication occurrence identity, with A1→B2→A3→retry acceptance, and explicitly preserves lookup keys for erased token-only restrictions until safe retirement. These are written contracts and future test obligations, not claims that software has passed them.

Non-blocking terminology cleanup remains useful: call the hash “content identity,” reserve “snapshot UUID” for occurrence identity, and align general rotation prose with active-key writes and prior-key lookup-only access.

# Improvements

Maintain one short reading index linking canonical requirements to data-model/interface/precision sections. When implementation begins, expand T057's requirement-to-test manifest with exact test IDs and evidence references. Generate closed API schemas, including explicit phone/mobile/landline mapping. Exercise multi-group merge and endpoint locking at the real database boundary alongside the specified suppression races.

# Final Plan

1. **Record the reviewed baseline.** Preserve `judge.md`, this merged plan, the input manifest and earlier reports. Run document/mirror checks after any later edit. Do not convert document-quality checkmarks into implemented task completion.
2. **Prepare the fixture foundation through T001–T013.** Create the isolated package, database, configuration, identity/provenance constraints, suppression/key authority, policy gates and spend ledger. Reject live credentials and network access in fixture mode.
3. **Build the controlled synthetic pilot.** Follow US1, US2 and minimal US4 dependencies. Prove permitted and denied candidates, current reviewer relevance, valid phone without email basis, immediate suppression, erasure/rotation, immutable row writes and uncertain CRM reconciliation. All provider I/O remains mocked until its explicit gate is satisfied.
4. **Build synthetic ABR integrity when scheduled.** Follow T035–T044 and the precision fixture list. Prove coherent generation hold, exact content identity, recurrence versus retry, zero-event baseline/repartition, versioned rebaseline and atomic recovery. Never invent production source-generation evidence or legacy rules.
5. **Collect release evidence separately.** Obtain applicable G1–G5 approvals, demonstrate operational/security/capacity controls, and complete independent handover. Start the live QBCC pilot only after its prerequisites; record G6 before live ABR expansion. G7 requires evidence for the actual release revision.
6. **Review the implementation independently.** Execute required boundary/fault tests, resolve actual findings and rerun affected checks. Preserve unrelated workspace changes. Roll back only task-owned changes if a proposed correction fails; do not erase historical approvals or review evidence.

Validation performed for this adjudication: `python tools/validate_abr_docs.py` exited 0 with 43 requirements, 59 unchecked build tasks and 43/43 mapped requirements. No application, source-ingestion, legal, benchmark or production test was executed or claimed.
