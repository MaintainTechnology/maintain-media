# Bounded closure audit

Reviewed 9 September 2026. Read-only review of `.runtime/finalize_acceptance.py`, the original T001–T065 checklist, `task-execution-review.md`, the fixed rubric, current trajectory, verification runner, document validator and the cited previous verification/runtime receipts. No full suite, live calls or closure script execution. This review changes only this acceptance artifact.

## Verdict

**57 locally completed tasks and eight open tasks is a defensible eventual scope, not the current repaired-tree result.** The eight open IDs match the task audit: T036, T041, T051, T053, T055, T058, T062 and T065. Keep the qualifications on the completed adapter/fixture tasks; this is not 57 fully production-certified obligations.

The previous receipt `verify-9c2124df83d846b2b9d50681ca78eb17/result.json` genuinely records a passed, unchanged tree `45f8fd096ecb7a4f7c84207b6914db94d8d44a0be6c038db81e4a9a7af68f592`. Independently parsed JUnit: 383 tests, zero failures, errors or skips. Its pytest command receipt elapsed 877.524 seconds; the test runner's own elapsed time is a different measurement. This evidence predates migration024 and cannot certify the repair. The helper's exact-tree assertion correctly prevents reusing that pass after changes; preserve that guard and select a fresh successful receipt.

## Confirmed closure corrections

1. **Reopened correctness finding prevents present closure.** Profile erasure losing historical pilot cohorts/outcomes affects T033/T050 and their recovery/acceptance coverage. Independent review and meaningful positive/negative retention, merge, correction and expiry regressions must resolve it before T059/T064 can close. Migration024 existing on disk is not evidence that this work passed. Reconcile the task audit's historical intermediate-result statements with a dated final addendum, rather than presenting those old rows as current final verdicts.

2. **The proposed plateau claim is not defensible after the finding.** `rubric.md` defines resolution of any high/critical correctness finding as a real improvement regardless of point delta. The draft helper's “last gains +1 then0” and “final pass changed only evidence/status” language omits the new correctness iteration. Reset the clean review streak, preserve the earlier pass as historical evidence, and explicitly record the repair and review. Keeping a score of92 does not require inflation; adding an evidence-only92 entry is insufficient by itself to demonstrate two subsequent substantive assessments with no actionable high/critical finding. Root has acknowledged this and will rewrite the helper.

3. **Open tasks contain buildable work, not only external approvals.** T036 still lacks the composed discovery → complete resumable resource download → metadata reread → archive-ingest operation. Existing injected download and coherence helpers do not make that composition present. It can be built with an explicitly supplied approved publisher mapping and synthetic transport while actual source discovery/schema approval and smoke remain open. Root assigned that bounded implementation after this finding. T055's73.34-second synthetic diff remains above the60-second stretch: further profiling is local work; target-host/full-XML evidence and approved plan adjustment remain separate. T062's hosted Linux execution and exact native package selection are deployment/tooling verification, not legal or commercial approvals. Do not say that all remaining deductions can only be resolved by elapsed pilot weeks or external contracts.

4. **Distinguish task evidence from requirement certification.** The helper attaches one hashed whole-suite receipt to each locally completed task, with the task-specific review row as supporting context. This is acceptable as combined command/revision/result evidence when the final suite actually includes the relevant tests. The requirement manifest is a scoped file map, not proof that every original live scenario ran. Preserve the explicit fixture limits for T019/T021/T024/T038/T045/T047–T049/T056/T057; keep T065 and full-spec acceptance open. Refresh the manifest/test references for the new retained-metrics regressions before final verification.

## Scope checks without a new defect

The currently read `revision_digest()` includes both Markdown and JSON feature files, so the generated task ledger is covered by the final content inventory. The document validator checks completed-task evidence paths, hashes, commands and result text. No ledger-hash omission is asserted by this review.

The stored final fixture run is complete with one selected, one blocked/deferred and zero live calls; its separate API receipt is passed. These are stored runtime observations, not a fresh exercise of migration024. The final report should identify their revision/date or rerun affected paths; retaining their artifact hashes alone does not turn them into new-revision evidence.

No additional confirmed high/critical runtime defect was established in this bounded closure review beyond the already reopened retention finding. That statement does not certify the whole codebase or establish the stopping plateau. The reviewer previously implemented output/CRM/monitor/propagation components; ownership and independence limits remain as disclosed in the task audit.

## Required final order

Finish and independently review the retained-metrics repair and newly assigned discovery composition; record focused results. Freeze the actual resulting code, configuration, tests, workflow and hash-tracked docs. Run the combined verification once against that tree. Reconcile task/status evidence, regenerate mirrors and verify that only the allowed documentation/ledger files changed afterward. Publish the scoped task count, outstanding implementation/evidence work and honest review trajectory from those actual results.

## Subsequent T036 disposition

Root assigned and reviewed the bounded discovery composition after this audit. `discovery-review.md` records the new code, independent source review, explicit unavailable metadata-response validators and58 passing focused discovery/source tests. The original implementation task T036 can now be locally complete after fresh final verification; actual source mapping, installed polling and live smoke remain T053/T058/T065. Accordingly the supported eventual count becomes **58 local complete / seven open**, not the initial57/8 proposal. The other closure corrections and reopened retained-metrics verification requirement remain unchanged.
