# Independent adaptive diff review — 8 September 2026

Reviewed root's numeric-range whole-set diff in `src/abr_engine/diff/events.py`. The bounded 200,000-row join is exercised independently against a 200,001-row dense key region plus a distant upper-bound key; tests also cover lower/upper keys, simultaneous event types, before/after payloads, baseline and no-op empty files, explicit parser rebaseline, and duplicate keys across input parts. These tests validate correctness and failure behavior, not full-register capacity.

**P1: failed final-output writes leak the part reader and leave a partial output that blocks retry.** The part `ParquetFile` closes only after all `writer.write_batch` calls succeed. A storage exception during writing reaches `part.unlink` while that reader remains open; on Windows, this raises WinError32 and replaces the original failure. The outer writer is closed in `finally`, leaving a readable partial `output_path`; subsequent execution refuses it as existing, so a resumable run cannot safely retry. Use a context-managed part reader; close final verification readers too. On failure after this function creates its owned output, remove the incomplete output only after closing readers/writers, while preserving an output that existed before invocation. Ensure per-part files are removed and retain the original exception where practical.

Initial independent result: **6 passed, 1 failed in 5.78s**. The failure injects an actual exception after a real Parquet write, not a mocked diff result. Root owns the implementation fix; this reviewer owns `tests/unit/test_diff_review.py` and will rerun the unchanged negative control afterward.

Fix rereview: root now closes part/final/iterator readers through context managers and removes an owned partial output after closing the writer on failure. The unchanged independent test suite passed **7 tests in 8.35s**, including retry after a real interrupted output write. This closes the recorded finding for the tested fixture failure boundary.
