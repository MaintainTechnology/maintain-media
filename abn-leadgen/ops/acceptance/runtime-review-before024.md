# Running build review — 9 September 2026

The current fixture run is `eed619d9-3b14-4dea-a920-d1a7fe2edc7b`; its redacted receipt is `final-runtime-run.json` and its immutable report bundle is recorded there. Both sources completed;1 selected business,1 blocked/deferred entry,0 live API calls. The previous held run is retained as `runtime-held-498c7767.json`. Its ABR file matched the original size/hash, but the pre011 registry lacked file-location metadata. The explicit recovery is recorded in `runtime-rebaseline.json`; it created a new zero-event ABR baseline with the supplied reason, then the new all-source run completed. No historical business events were invented.

The current hidden loopback control process on `http://127.0.0.1:8766` passed actual HTTP checks in `runtime-api-37738cab704b4ebcb93a2111b72478a1.json`: health200 and fixture/outreach-disabled, anonymous schema401, authenticated schema/worklist200, new synthetic opt-out201 and identical replay200. A separate database transaction observed the committed restriction. Measured client request through returned commit acknowledgement was0.216569s. This is one local fixture sample, not a production SLA. Tokens and the synthetic endpoint were not written to the acceptance receipt.

The current report is open at `http://127.0.0.1:8767/report.html` in the Codex browser, serving only the synthetic report directory. Actual360x800 viewport check: `innerWidth=360`, `scrollWidth=345`, no horizontal overflow. The permitted `Email: needs send-time checks` label and potential-duplicate review hint are present. Tab focuses the CSV link with a visible purple focus ring. All eight definition labels occur once in the DOM; the browser's stitched full-page screenshot repeated a capture overlap, not document elements. The ordinary viewport was restored and the report tab marked as a deliverable.

The report is an internal review artifact, not a public dashboard or a sender. Local loopback preview provides no production access-control certification. Hosted CI, live sources/vendor contracts, AU deployment, Linux scheduler and measured pilot remain separate pending requirements.
