# GoHighLevel phone-only pilot technical admission — release 009b

Status: approved for the bounded phone-only DND account admission under the user's express delegation. Activation must still verify the exact reviewed plan and current host preconditions; this document does not claim that activation has already occurred.

This decision admits the existing Maintain Media HighLevel location `xHZFHMOE476t5CxY9vCG` for individually approved, selected tier A phone records with Do Not Disturb always on. It does not approve a real business, grant calling permission, enable outreach, enable Google Sheets, or certify the entire product for general production.

The user's express delegation supplies the business decision. The separate final GHL owner record identifies the accepted vendor countries, limitations and finite purpose. Jon's or an adviser's approval is not claimed. The existing immutable QBCC and website decisions remain unchanged; this record does not invent additional source permission.

## Reviewed implementation

- Source archive SHA256: `cbee38f72c057450d3ba3a3068877a917f1918b8f63450f0605e1bb46f314d20`.
- Public manifest SHA256: `7b1b866ec895c3fbe84409113602859200f0fd83cc18576d14bc35db8dc8f8c9`; 153 files, existing frozen dependency lock and 26 migrations.
- CRM config SHA256: `d9353beca5e3490fa7aa6f249e6ce1c784193337de04cd0a4515b199a3339477`.
- Installation SHA256: `3ba2247eb3ffa495e0a890ebcf02e05e1e5f9bcc5be3d5de7e0e525fab82eeec`.
- Actual account contract receipt SHA256: `b8163433480fa379bda245bcc14c30b928ba0e3e8884a986994635554ea42140`.
- Aggregate backend review SHA256: `55455c2f0209c57b9bfee47735a4c4482ea5294447f68b8d2fa3fd8cc3226cd3`; 106 distinct passing scenarios, with the earlier corrected test-fixture failure preserved explicitly.

The provider stays pinned to API version `2023-02-21`, a fixed HTTPS host, bounded responses and explicit endpoints. The config restricts disclosure to one phone value, the 13 reviewed custom fields, business name, engine tags and DND state. No email value or raw evidence is exported. The leased worker claim for the durable approved outbox and current row revision are rechecked before each provider mutation. The worker cannot turn a collected contact into an approved lead.

Actual HighLevel GET responses split a submitted business name into first and last fields. The adapter now reconstructs that name without using lowercase indexes or unrelated company metadata. Suppression explicitly clears both derived fields and the engine-owned projection. Actual account readback confirmed clearing, DND and preservation of an unrelated test tag.

Every mutation checks the current complete workflow inventory against the reviewed all-draft SHA256 `e30e1d90e03a629a82b04bf0d552322e9e8f47add0fe2967e81f3fbac72291e8`, then repeats the local authority check. A published, changed, incomplete, unknown or unavailable inventory holds the write. An authorised administrator can still change the account between the GET and mutation; this is not an atomic vendor lock. Account administration must preserve isolation. A changed inventory also holds removal until a newly reviewed config and G5 restore it; local restrictions take effect immediately.

## Actual account verification

The signed-in operator is listed as an agency administrator in the correct workspace. Duplicate contacts are disabled, with email then phone matching. The private integration has only location read, custom-field read, contact read/write and workflow read scopes. No token was rotated or exposed. Contact write scope is broader than this application's endpoint allowlist; the application never calls message, campaign or workflow-enrolment endpoints.

The complete pinned API inventory contains six draft workflows. Their browser list showed zero total and active enrolments before and after testing. The subaccount's installed marketplace app list showed zero. These are time-bound observations, not an assertion about every future staff action or optional vendor service.

Two clearly labelled synthetic records used an ACMA-reserved fictional number. The real API verified exact field mapping, group search, duplicate rejection without overwrite, DND, suppression clearing, unrelated-tag preservation and reconciliation after an intentionally lost successful response. Both test records were cleared and deleted, and missing-record reads confirmed removal from the live contact API. No real lead was exported and no communication or enrolment was requested. Provider backup erasure is not claimed.

The lost-response test observed an initially empty search index followed by the exact existing record. Uncertain creates remain held and never blindly create again. An adjacent update-recovery defect was fixed: an already-known provider ID is fetched and checked when indexed search is temporarily empty; a missing or conflicting record holds instead of creating another contact. Six targeted PostgreSQL recovery checks passed.

## Hosting, notice and continuing removal

The existing Sydney service, private runtime credentials, restricted database role and signed request boundary remain in place. New immutable config and evidence files are readable only by root and the service group. Runtime database credentials cannot write policy or release authority. Deployment preserves release 008 and all existing data. Restoring 008 requires stopping CRM work and disabling the CRM capability first because 008 lacks this reviewed account contract and recovery behavior.

The updated dashboard and public research notice are deployed as `dpl_4uugYiVhLyYZgKVeVMta1Wxcb7HZ`. Forty focused frontend tests, lint, TypeScript and the production build passed. Twelve public/signed-out HTTP checks passed. The exact notice GET passed 21 content checks; body SHA256 is `c6df5769df900cf8f4cddd9979164d870ca47f635de858fa3e7958c915405fe3`. Sixty-five ordinary function outputs use Sydney; existing Clerk middleware remains global and the build region is separately recorded. A fresh authenticated staff-browser journey was not re-certified.

Acquisition G1/G7 ends on `2026-09-24T23:18:20Z`. The installation and G3/G5 remain valid only for bounded maintenance/removal through `2027-09-10T23:18:20Z`; acquisition still requires the unexpired G1/G7 and current parent collection authority. This prevents pilot expiry from automatically eliminating the ability to clear an existing CRM record. The existing phone-only purpose and 180/90/30-day retention rules remain; export does not reset a record's clock, and selected-evidence archiving stays off.

Independent recovery-key custody, a full restore exercise, activated backups, external alert delivery, the 100-business classifier review and measured elapsed pilot remain unresolved. The private Sheet workflow stays off. Real phone qualification also lacks a live DNCR receipt adapter and genuine current wash evidence, reviewed locality/timezone and selected row approval. These are explicit remaining product gaps; this account admission does not label them passed.

## Activation limits

The activation helper must verify exact source, owner, technical, config, installation and all seven check hashes, the unchanged 19-gate/two-policy baseline, one business/two phones, no selected rows or CRM outbox, and stopped units. It appends four CRM gates only and changes the CRM capability plus its two config paths. Existing acquisition, website and retention settings are preserved; no service or schedule is enabled by the helper. The weekly source and backup timers remain disabled.

The exact staged release passed 107 core and 74 GHL/installation Linux tests, with zero failures or skips. They ran as the service user against the actual staged Python import under a separate network namespace containing only loopback; the existing live 008 release stayed unchanged throughout validation. Two existing dependency deprecations in the core run are not hidden. The activation helper separately passed 32 distinct checks, including actual isolated-PostgreSQL authority append/readback and negative evidence/configuration cases.

The reviewed activation-plan hash must be recorded before admission. Live verification must subsequently confirm the signed dashboard, current CRM contract, restricted unsigned access, zero unintended outbox/actions and healthy maintenance services.
